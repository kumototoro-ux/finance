# مرجع شامل — منصة مِرقاة التعليمية (موقع الموظفين)

**الغرض من هذا الملف**: توثيق كامل لكل ما بُني بموقع الموظفين، ليُستخدَم كمرجع أساسي عند بناء المواقع المتصلة الجديدة (موقع الإدارة المالية، موقع الطلاب وأولياء الأمور) — كل موقع بمستودع منفصل، لكن متصل بنفس قاعدة بيانات Supabase.

---

## 1) نظرة عامة على المشروع

- **الاسم**: منصة مِرقاة التعليمية (SEAP) — نظام SaaS لإدارة مدرسة متعددة الفروع (مدرسة دار الهدى، 9 فروع).
- **العميل الحالي**: أنس ال شائف (الأدمن، E001) — يدير تطوير المنصة بنفسه لصالح مدرسته، بهدف تحويلها لمنتج SaaS متعدد العملاء مستقبلاً.
- **الانتقال**: هجرة كاملة من Google Apps Script/Sheets لمنصة ويب حديثة.
- **الحالة**: موقع الموظفين (هذا المستودع) شبه مكتمل وظيفياً بمعظم الأنظمة الأساسية. الخطوة التالية: بناء موقع الإدارة المالية وموقع الطلاب/أولياء الأمور كمستودعات منفصلة متصلة بنفس القاعدة.

---

## 2) البنية التقنية والمعمارية

| الطبقة | التقنية |
|---|---|
| الواجهة الأمامية | JavaScript خالص (Vanilla JS/HTML/CSS) — SPA، بلا أي إطار عمل (لا React/Vue) |
| الاستضافة | Vercel (نشر تلقائي من GitHub) |
| الخلفية | Vercel Serverless Functions (Node.js ESM) |
| قاعدة البيانات | Supabase PostgreSQL — `service_role` key، بلا Supabase Auth |
| المصادقة | JWT مخصَّص (توليد يدوي بمكتبة `jsonwebtoken`)، مدة الجلسة 6 ساعات |
| التخزين المؤقّت لتحديد المعدَّل | Upstash Redis (5 محاولات دخول/60 ثانية) |
| GitHub | `kumototoro-ux/SEAP` (main branch) |
| النشر | `seap-delta.vercel.app` |
| PDF | `html2pdf.js` عبر CDN (المكتبة الخارجية الوحيدة المُضافة) |

### القيد المعماري الأهم: **12 ملف API بالضبط**
خطة Vercel Hobby (المجانية) تفرض حداً أقصى صارماً. الملفات الاثنا عشر الحالية:

```
api/
  auth.js            — تسجيل الدخول، الجلسات، تغيير كلمة المرور الإجباري
  employees.js        — إدارة الموظفين
  students.js         — إدارة الطلاب
  parents.js           — إدارة أولياء الأمور
  family-accounts.js  — حسابات دخول الطلاب/أولياء الأمور (لموقع الطلاب المستقبلي)
  users.js            — حسابات دخول الموظفين
  settings.js         — الإعدادات العامة + القوائم القابلة للتعديل
  attendance.js       — تحضير الطلاب والموظفين (نظام مُعاد بناؤه بالكامل)
  behavior.js         — سلوك الطلاب
  performance.js      — تقييم أداء الموظفين
  audit-log.js        — سجل التتبّع + نظام المراسلات الكامل
  academic-config.js  — الملف "الضخم": التقويم، الجداول، التكاليف، الرصد، المشاركة، أداء الطلاب، الإحصاءات
```

**قاعدة ثابتة لا تتغيّر**: أي ميزة جديدة = إجراء (action) داخل أحد هذي الملفات الاثني عشر، **لا ملف جديد أبداً**. `academic-config.js` أصبح يحتوي عشرات الإجراءات المختلفة لهذا السبب بالضبط.

### `lib/` — طبقة مشتركة
```
lib/
  auth.js          — requireAuth(), requireRole(), AuthError (401=مصادقة فعلية، 403=صلاحية)
  router.js        — createRouter() يوزّع كل طلب لدالة handler حسب action
  apiHandler.js    — يغلّف كل شيء بمعالجة أخطاء موحَّدة
  validation.js    — كل مخططات Zod (يكبر مع كل ميزة جديدة)
  supabaseAdmin.js — عميل Supabase بصلاحية service_role
  idGenerator.js   — توليد معرّفات نصية (STU-XXXXXX، EMP-XXXXXX)
  rateLimit.js     — تحديد معدّل الدخول عبر Upstash Redis
```

⚠️ **ملاحظة معمارية مهمة**: كل ملف API **مستقل تماماً** بخادمه — لا يمكن لملف يستخدم دالة معرَّفة بملف آخر بلا نسخها صراحة (اكتُشِف هذا بالخطأ أكثر من مرة: `resolveTermAndWeek_` كانت معرَّفة بـ`attendance.js` فقط واستُخدِمت بالخطأ بـ`academic-config.js` بلا نسخها، فسبَّبت `ReferenceError` بالإنتاج).

### `public/` — الواجهة
```
public/
  index.html          — نقطة الدخول، يحمّل كل شيء بـ<script>/<link> عادي
  js/
    app.js            — **الملف المحوري** — كل منطق الواجهة (+7000 سطر الآن)
    api.js            — apiCall() الموحَّدة لكل طلبات الخادم
    icons.js          — كل الأيقونات كدوال SVG (ICONS.name())
  css/
    style.css         — أنماط أساسية (تصفير، متغيّرات الألوان)
    dashboard.css     — كل أنماط لوحة التحكم (+2000 سطر الآن)
sql/                  — كل ملفات SQL يدوية التنفيذ (لا Migration تلقائي)
```

---

## 3) قاعدة البيانات — كل الجداول (الحالة النهائية بعد كل التعديلات)

### جداول الأشخاص الأساسية
- **`employees`** — `id` (TEXT، مثال "E001")، `name_ar`, `role`, `branch`, `username`, `password_hash` (بجدول `users` منفصل فعلياً)، `deleted_at` (Soft Delete)
- **`employee_branches`** — فروع إضافية لمراقب الفروع (بجانب `employees.branch` الأساسي)
- **`students`** — `id` (TEXT، "STU-XXXXXX")، `national_id`, `name_ar`, `branch`, `stage`, `grade`, `section`, `subjects` (مصفوفة — تُسحَب تلقائياً من `subject_distribution_matrix`)، `fee_status`, `deleted_at`
- **`parent_info`** — أولياء الأمور، `deleted_at`
- **`parent_student_links`** — ربط ولي أمر بطالب (many-to-many)
- **`family_accounts`** — حسابات دخول الطلاب/أولياء الأمور (لموقع الطلاب المستقبلي) — ⚠️ يحمل خطأ إملائي داخلي ثابت `role_studen` (بدل `role_student`) مستخدَم بثبات بكل مكان بالملف — **لا تصلحه بلا ترحيل بيانات منسَّق**، يعمل صح كما هو
- **`users`** — حسابات دخول الموظفين (`password_hash`, `password_changed_at`)

### الإعدادات
- **`site_settings`** — صف واحد فقط (`id=1`): `school_name`, `logo_url`, `allow_message_images`
- **`settings_lists`** — قوائم قابلة للتعديل (`list_key`, `value`, `sort_order`) — تغطي: الفروع، المراحل، الصفوف، الشعب، المواد، أنواع المستخدمين، الأدوار، حالات الحساب، حالات الحضور، الفصول الدراسية، حالات السلوك، أنواع التقييم المستمر، الاختبارات

### التقويم الدراسي
- **`academic_terms`** — `name`, `academic_year`, `start_date`, `end_date`, `is_visible`
- **`academic_weeks`** — `term_id`, `label`, `week_number`, `week_type`, `start_date`, `end_date`
- **`academic_holidays`** — `label`, `start_date`, `end_date`

### الجداول الدراسية
- **`class_schedule_slots`** — `teacher_id` (TEXT)
- **`exam_committee_schedule`** — `supervisor_id` (TEXT)

### التكاليف والاختبارات (نظام أصلي — **لا يُلمَس**، مكتمل حسب توضيح صريح من العميل)
- **`assignments`** — `teacher_id`, `title`, `category` (تكليف/مهمة/اختبار/إثراء)، `subtype`, `subject`, `grade`, `section`, `eval_type`, `max_score`, `available_from`, `due_at`, `published_at`
- **`assignment_questions`** — `answer_type` (mcq/true_false/short_answer/long_answer/attachment)، `points`، خيارات MCQ
- **`assignment_grades`** — `score`, `max_score`, `recorded_at`, `reopened_at`, `reopened_by` (نافذة تعديل يوم كامل)

### نظام الرصد الجديد (كشوفات الرصد المباشر — أُنشئ بهذي الجلسة)
- **`grading_sheets`** — كيان مستقل: `teacher_id`, `branch/stage/grade/section`, `subject`, `eval_type`, `title`, `description`, `max_score` (Snapshot ثابت يحدّده المعلم يدوياً دائماً — **لا علاقة له بوزن الإعدادات**)، `term_id`, `week_id`, `record_date`, `status`, `reopened_at`, `reopened_by`
- **`grading_sheet_entries`** — `sheet_id`, `student_id`, `score`, `note`

### توزيع الدرجات والمشاركة
- **`grade_distribution`** — `subject`, `eval_type`, `max_grade` (الوزن من 100 — **مرجع تجميع نهائي ثابت، لا يتغيّر بتغيّر الدرجة القصوى لأي تكليف فردي**)، `is_participation` (يحدّد أي نوع يمثّل "المشاركة والتفاعل" فعلياً — لا اختيار حر بالواجهة)
- **`participation_log`** — `student_id`, `subject`, `eval_type`, `direction` (positive/negative — تصنيف وصفي فقط)، `participation_type` (مشاركة/إجابة/تفاعل/نشاط/مبادرة/أداء متميز)، `session_max_score`, `earned_score` (الحساب الفعلي)

### الحضور (نظام أُعيد بناؤه بالكامل بجدول جديد)
- **`attendance_records`** (الجدول الجديد — استبدل `attendance` القديم المليء بأعمدة `NOT NULL` قديمة معطوبة): `person_id`, `person_type`, `branch`, `stage`, `grade`, `section`, `target_role`, `date`, `status`, `term_id`, `week_id`, `recorded_by`, `updated_by`
  - قيد `UNIQUE (person_id, person_type, date)` حقيقي بقاعدة البيانات — يمنع أي تكرار

### السلوك والتقييم
- **`student_behavior`** — نقاط 100 أساس، خصومات
- **`evaluation_cycles`**, **`evaluation_criteria`**, **`evaluation_scores`** — تقييم أداء الموظفين

### المراسلات (نظام "بريد إلكتروني" أُعيد بناؤه بالكامل)
- **`chat_threads`** — `subject`, `context_type` (general/grade_reopen_request/sheet_reopen_request)، `context_id`, `sender_id`, `sender_type`, `branch`
- **`chat_recipients`** — `thread_id`, `recipient_id`, `recipient_type` (employee/student/parent)، `read_at`
- **`chat_messages`** — `thread_id`, `sender_id`, `body`, `image_url`, `is_original`, `read_at`
  - ⚠️ **لا يوجد حذف تلقائي للردود بعد الآن** (كان موجوداً سابقاً بقيد 24 ساعة، أُلغي عمداً لصالح أرشيف دائم)
- **`blocked_terms`**, **`blocked_senders`** — فلترة كلام محظور + حظر تلقائي

### تجميع النتائج
- **`grade_aggregation_results`** — نتيجة محسوبة تلقائياً بعد كل رصد (تكليف أو كشف أو مشاركة): `student_id`, `subject`, `branch/stage/grade/section`, `final_grade`, `breakdown` (JSONB — تفصيل شفاف لكل نوع تقييم ومصدره)

### سجل التتبّع
- **`audit_log`** — `emp_id`, `emp_name`, `role`, `action`, `details` (JSONB)، `branch`, `created_at`

---

## 4) خارطة كل الصفحات (Frontend) — بالترتيب بالقائمة الجانبية

| المفتاح | التسمية | الوظيفة |
|---|---|---|
| `home` | الرئيسية | ساعة فاخرة (تناظرية/رقمية)، تقويم مصغَّر، بطاقات KPI ملوَّنة، شبكة وصول سريع، آخر الأنشطة، أقرب المواعيد |
| `academicCalendar` | التقويم الدراسي | عرض شهري/أسبوعي/فصل كامل، إدارة فصول/أسابيع/إجازات (أدمن) |
| `schedules` | الجداول الدراسية | جدول حصص تفاعلي + جدول اختبارات |
| `assignments` | التكاليف والمهام | إنشاء تكاليف/مهام/اختبارات/إثراء، أسئلة متعددة الأنواع، تصحيح آلي عند التفعيل — **مكتملة، لا تُعدَّل** |
| `assignmentGrades` | **الرصد والتصحيح** | 3 تبويبات: (1) الرصد الموحَّد (تكاليف + كشوفات مباشرة معاً)، (2) المشاركة والتفاعل، (3) تعديل الكشوفات |
| `studentPerformance` | أداء الطلاب | 3 تبويبات: لوحة تحليل مرنة (فلاتر بحاويات + KPI + رسوم + مقارنات)، تقرير طالب فردي، ملخّص صف/شعبة |
| `registrationStats` | إحصائيات التسجيل | إحصاءات طلاب/موظفين/أولياء أمور — أدمن (كل الفروع) + Admission (فرعها) |
| `employees` | الموظفون | CRUD كامل |
| `students` | الطلاب | CRUD + **فلاتر إجبارية** (فرع/فلتر إضافي/بحث) قبل عرض أي بطاقة |
| `parents` | أولياء الأمور | CRUD + ربط بالطلاب |
| `familyAccounts` | حسابات الطلاب والأسر | إدارة حسابات دخول موقع الطلاب المستقبلي |
| `users` | المستخدمون | حسابات دخول الموظفين، إعادة تعيين كلمة مرور |
| `auditLog` | سجل التتبّع | 8 تبويبات تصنيف تلقائي + فلاتر (دور/فرع/فترة) |
| `siteSettings` | الإعدادات | معلومات عامة، الهيكل الأكاديمي، التقييم والاختبارات، توزيع الدرجات (بطاقات لكل مادة)، توزيع المواد، الحسابات والأدوار، الحضور والسلوك، التقويم الدراسي |
| `studentAttendance` / `staffAttendance` | تحضير الطلاب/الموظفين | يعمل على `attendance_records` الجديد، مرتبط تلقائياً بالفصل/الأسبوع الدراسي |
| `studentBehavior` | سلوك الطلاب | نقاط 100 أساس |
| `performance` | تقييم الأداء | معايير مرجَّحة، دورات تقييم، KPI بالرسوم |
| `messages` | **المراسلات** | تجربة بريد إلكتروني كاملة: وارد/مرسَل/غير مقروء، بحث، صفحة قراءة كاملة، رد، صور، بحث ذكي عربي مرن للمستلمين |
| `reports` | التقارير | تقارير موحَّدة برسوم بيانية، تصدير PDF احترافي |

---

## 5) خارطة الصلاحيات الكاملة (كل دور)

```
role_admin:          كل الصفحات، بلا أي قيد فرع/صف/شعبة
role_teacher:        الرئيسية، التقويم، الجداول، التكاليف (تكاليفه فقط)، الرصد،
                      أداء الطلاب (طلابه فقط: مادة+صف+شعبة)، المراسلات (طلابه
                      وأولياء أمورهم + موظفي فرعه + كل الأدمن)، تحضير الطلاب،
                      تقييم الأداء (عرض)
role_student_sup:    الرئيسية، التقويم، الجداول، التكاليف (عرض)، أداء الطلاب
                      (فرعه)، المراسلات (فرعه: موظفين+طلاب+أولياء أمور)،
                      تحضير الطلاب، سلوك الطلاب، تقييم الأداء، التقارير،
                      الطلاب/أولياء الأمور/حسابات الأسر (فرعه)
role_teacher_sup:    الرئيسية، التقويم، الجداول، التكاليف (عرض)، أداء الطلاب
                      (فرعه)، المراسلات (موظفي فرعه فقط)، تحضير الموظفين
                      (معلمين فقط)، تقييم الأداء، التقارير
Admission:           الرئيسية، التقويم، إحصائيات التسجيل (فرعها)، المراسلات
                      (فرعها: موظفين+طلاب+أولياء أمور)، الطلاب/أولياء
                      الأمور/حسابات الأسر (فرعها فقط)
role_branch_monitor: الرئيسية، التقويم، الجداول، التكاليف (عرض)، أداء الطلاب
                      (فروعه)، المراسلات (فروعه: موظفين+طلاب+أولياء أمور،
                      بتدفّق: فرع←نوع←بحث)، تحضير الموظفين (مشرفَين فقط)،
                      تقييم الأداء، التقارير — عبر user.allBranches
```

### مبدأ إنفاذ الصلاحيات (ثابت بكل الأنظمة المبنية)
1. **الخادم هو مصدر الحقيقة الوحيد** — كل فحص صلاحية يُعاد تطبيقه بالخادم بغض النظر عمّا تُظهره الواجهة (حماية من التلاعب المباشر بالطلبات).
2. **الفرع يُفرَض تلقائياً** لغير الأدمن بكل استعلام حسّاس — لا يُقرَأ من طلب العميل أبداً لهذا الغرض.
3. **401 = مصادقة فعلية فقط** (توكن غير موجود/منتهٍ)، **403 = رفض صلاحية** (جلسة سليمة لكن إجراء خارج النطاق) — الخلط بينهما كان ثغرة حقيقية أُصلِحت (كانت تُخرِج المستخدم بالكامل لمجرد محاولة إجراء عادي خارج صلاحيته).

---

## 6) نظام التصميم (Design System)

### الألوان الأساسية (متغيّرات CSS)
```css
--primary: أخضر داكن (هوية المنصة)
--surface / --surface-card: خلفيات فاتحة متدرّجة
--outline: حدود خفيفة
--text-muted: نص ثانوي رمادي
```
ألوان دلالية: أخضر (#2F7A4D) نجاح/إيجابي، أحمر (#C4483A) خطر/سلبي، ذهبي (#B8860B/#C9A24B) تحذير/فاخر، أزرق (#3E7CB1) معلومات، بنفسجي (#7B5FB8) تمييز.

### المكوّنات المُعاد استخدامها بكل الموقع
- **`.field`** — غلاف موحَّد لكل حقل إدخال (label + input/select/textarea) — حلقة تركيز خضراء عند focus، سهم منسدل موحَّد للقوائم، استثناء صريح لـcheckbox/radio من التنسيق العام (درس مستفاد: **لا تُطبِّق تنسيق النصوص على عناصر الاختيار**).
- **منتقي تاريخ مخصَّص بالكامل** (`buildCustomDatePicker_`) — الحل النهائي بعد 3 محاولات فاشلة بالاعتماد على `<input type="date">` الأصلي (عطل RTL معروف بـChrome Android). الحقل الأصلي مخفي تماماً (`display:none`) لكن موجود بالـDOM لتوافق كل الكود القارئ لـ`.value`، وطبقة عرض/تقويم مخصَّصة فوقه بالكامل تحت التحكّم الكامل.
- **`showConfirmDialog_()`** — نافذة تأكيد احترافية (Promise-based) بديلة عن `confirm()` المتصفح البدائي.
- **`showProfessionalReportShell()`** — قشرة تقرير موحَّدة (رأس بشعار المنصة/المدرسة + معلومات المُعِد + علامة مائية رادعة + تصدير PDF/مشاركة) تُستخدَم بكل صفحات التقارير.
- **`renderBarChartSVG` / `renderDonutChartSVG` / `renderGaugeSVG`** — رسوم بيانية HTML/CSS أو SVG بحجم ثابت (**درس مهم**: أي رسم بحجم نسبي لعرض الحاوية يتشوّه على الشاشات العريضة جداً — استُبدِل الشريطي بالكامل بـHTML/CSS بدل SVG لهذا السبب بالضبط).
- **الساعة الفاخرة** (`buildLuxuryClockSVG`) — SVG بالكامل، الزاوية مُدمَجة مباشرة بنص SVG وقت الإنشاء (لا `setAttribute` لاحق — كان يفشل بصمت).
- **بطاقات KPI ملوَّنة** بتدرّجات لونية (`.home-kpi-accent-*`) بدل الأبيض المتطابق.
- **حاويات فلاتر مستقلة** (`.analytics-filter-card`) — تُضاف فقط عند الحاجة عبر زر "+ إضافة فلتر"، بدل عرض كل الفلاتر دفعة وحدة.

### القاعدة الذهبية بكل تعديل CSS
**لا تنسَ `padding: 0` على أي `<button>` بحجم دائري/مربَّع صغير** — القاعدة العامة `button { padding: 11px 20px }` كسرت أزرار التقويم، منتقي التاريخ، وأيام الشبكة **3 مرات منفصلة** بهذي الجلسة لنفس السبب بالضبط.

---

## 7) القرارات المعمارية الثابتة (لا تتغيّر بأي موقع جديد)

1. **ملفات كاملة دائماً** عند التسليم — لا تعليمات بحث-واستبدال جزئية مهما كان حجم الملف.
2. **حد أقصى صارم لعدد ملفات API** (12 بهذا المستودع تحديداً — الموقع الجديد له حد مستقل حسب خطته بـVercel).
3. **SQL يدوي التنفيذ دائماً** (`CREATE TABLE IF NOT EXISTS`, `ALTER TABLE ADD COLUMN IF NOT EXISTS`) — لا Migration تلقائي، العميل ينفّذ بـSupabase SQL Editor بنفسه.
4. **Soft Delete فقط** للموظفين/الطلاب/أولياء الأمور (`deleted_at`).
5. **حماية على مستوى الكود** لا RLS — `service_role` + `requireAuth`/`requireRole` بكل handler.
6. **SVG بحجم ثابت أو HTML/CSS نسبي** — أبداً SVG بـ`viewBox` صغير و`width:100%` (يتشوّه على الشاشات العريضة).
7. **الدرجة القصوى لأي تكليف/كشف/مشاركة = قرار المعلم دائماً لحظة الإنشاء** — منفصلة كلياً عن وزن نوع التقييم بالإعدادات (مرجع تجميع نهائي ثابت فقط). **هذا كان أخطر سوء فهم بالجلسة كاملة** — أي موقع جديد يتعامل مع درجات يجب أن يحترم هذا الفصل الصارم.
8. **فحص توازن الأقواس/التعليقات فوراً بعد كل تعديل** (`awk '/\/\*/{c++} /\*\//{c--}'` + `node --check`) — بروتوكول إلزامي منع عشرات الأخطاء المحتملة هذي الجلسة.
9. **إجراء خادم واحد لا يخدم أكثر من صفحة بمنطق مختلف** — الدرس المستفاد من ثغرة `students:list` (فُرِضت عليها بوابة فلاتر لصفحة إدارة الطلاب فقط، فكسرت كل صفحة أخرى تستخدم نفس الإجراء داخلياً).

---

## 8) دروس أمنية حرجة (لا تتكرر بالمواقع الجديدة)

1. **IDOR بالمراسلات**: أي `threadId` كان يُقبَل بلا تحقّق ملكية — أُصلِح بـ`assertThreadParticipant_()` إلزامية بكل قراءة/رد.
2. **401 مقابل 403**: `requireRole` كانت ترمي 401 دائماً (بدل 403) — أي رفض صلاحية بسيط كان يُخرِج المستخدم بالكامل من الجلسة الحية عبر منطق الواجهة الخاطئ (تعامل مع 401و403 كواحد).
3. **تجاوز التغيير الإجباري لكلمة المرور بتحديث الصفحة**: التوكن كان يُحفَظ بـ`localStorage` قبل إتمام التغيير الفعلي — أُصلِح بإبقائه بالذاكرة فقط (`authToken` صريح لـ`apiCall`) لحين النجاح.
4. **القيد الشهري لتغيير كلمة المرور يرث بعد إعادة تعيين الأدمن**: `password_changed_at` لازم يُصفَّر صراحة عند أي إعادة تعيين إدارية.
5. **البحث عن مستلمي المراسلات** يجب أن يكون **مقيَّداً بالاستعلام نفسه** (لا فلترة بعد الجلب) — نمط مطبَّق بكل الأنظمة: `searchMessageRecipients`, `searchStudentsForPerformance`, إلخ.
6. **تحقّق خادم مستقل عن الواجهة دائماً** لكل عملية كتابة حسّاسة (تسجيل طالب بفرع، مراسلة شخص، تعديل درجة) — لا تكتفِ بإخفاء الخيار بالواجهة فقط.

---

## 9) الحالة الحالية والمعلَّق

### ✅ مكتمل وشُغِّل بنجاح
كل ما بالقسم 4 أعلاه، زائد: شاشة دخول احترافية (Splash 5 ثوانٍ بمرحلتَين)، خروج تلقائي بعد 15 دقيقة خمول، جرس إشعارات بحركة رنين، أمان جلسات مُدقَّق.

### 🟡 معلَّق (حجمه كبير، يحتاج جولة/موقع منفصل)
- **موقع الطلاب وأولياء الأمور**: البنية التحتية جاهزة (`family_accounts`, صلاحية `role_studen`) لكن الموقع نفسه لم يُبنَ بعد — هذا موضوع المحادثة القادمة.
- **موقع الإدارة المالية**: لم يبدأ إطلاقاً.
- **Drill-down تفاعلي حقيقي** بلوحة تحليل الأداء (الضغط على الرسم نفسه لتعميق التحليل) — حالياً تعديل الفلاتر يدوي.
- **رفع صور فعلي بالمراسلات**: يدعم حالياً رابط جاهز فقط (`allow_message_images` بالإعدادات) — رفع ملف مباشر يحتاج تفعيل Supabase Storage (غير مُفعَّل بالمشروع).
- **مراجعة إجابات الطلاب الفعلية بالتصحيح الآلي**: البنية جاهزة، لكن لا تنفيذ فعلي (لا يوجد موقع طلاب بعد يُدخِل إجابات).
- **الصفحة الرئيسية**: تحسين بصري أُنجِز، لكن لم تُطابَق التصاميم المرجعية المتقدّمة (SaaS Dashboards احترافية) بالكامل.

---

## 10) ملاحظة للمواقع الجديدة (المالية + الطلاب)

بما إن كل موقع جديد سيكون **مستودعاً منفصلاً** لكنه **متصل بنفس قاعدة Supabase**:
- **جدول `chat_threads`/`chat_recipients`/`chat_messages`** مصمَّم أصلاً ليكون **مركزياً** — `sender_type`/`recipient_type` يدعمان بالفعل `employee`/`student`/`parent`، فيمكن لموقع الطلاب إرسال/استقبال رسائل بنفس الجداول مباشرة.
- **`family_accounts`** جاهز لتسجيل دخول الطلاب/أولياء الأمور — يحتاج موقع الطلاب فقط بناء منطق مصادقة مشابه لـ`auth.js` الحالي (JWT مخصَّص) يقرأ من نفس الجدول.
- **`grade_aggregation_results`** يمكن لموقع الطلاب قراءته مباشرة (بلا أي كتابة) لعرض درجات الطالب لنفسه.
- **موقع الإدارة المالية** لا يوجد له أي جدول جاهز بعد — يحتاج تصميم مخطط جديد بالكامل، لكن يمكنه القراءة من `students`/`parent_info`/`employees` لربط البيانات المالية بنفس الأشخاص.
- **احترم نفس القيود المعمارية** (حد ملفات API حسب خطة الاستضافة، Soft Delete، حماية بمستوى الكود، SQL يدوي) ما لم يقرّر العميل غير ذلك صراحة.
